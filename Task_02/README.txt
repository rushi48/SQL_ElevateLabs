# SQL Developer Internship – Task 2

**Objective:**  
Practice inserting, updating, deleting data in a SQL database and handling NULLs.

---

## 📂 Files
- `task_02.sql`: Contains all SQL statements.
- `README.md`: Documentation of what was done.

---

## 🧠 Key Learning Points
- Used `INSERT INTO` to add new rows.
- Practiced inserting partial columns to leverage `DEFAULT`.
- Updated existing records with `UPDATE`.
- Deleted records with `DELETE`.
- Inserted new data using `INSERT ... SELECT`.
- Queried data to check for NULLs.

---


